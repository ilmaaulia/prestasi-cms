import { Route, Routes } from 'react-router-dom'

const ProfileStudentRoute = () => (
  <Routes>
    <Route index element={<div>Halaman profil mahasiswa belum tersedia</div>} />
  </Routes>
)

export default ProfileStudentRoute
